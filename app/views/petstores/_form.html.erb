<%= form_for(@petstore) do |f| %>
  <% if @petstore.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@petstore.errors.count, "error") %> prohibited this petstore from being saved:</h2>

      <ul>
      <% @petstore.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
